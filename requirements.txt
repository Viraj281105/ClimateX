# --- Core API and Server ---
fastapi==0.120.2
uvicorn[standard]==0.38.0

# --- Data Handling and Science ---
pandas==2.3.3
scikit-learn==1.7.2
joblib==1.5.2

# --- Database and Configuration ---
pymongo==4.15.3
python-dotenv==1.2.1

# --- LLM and Local Services (Ollama Client) ---
# Note: The 'ollama' Python client is typically installed directly,
# but we list related libraries that provide model functionality.
# You will likely need to ensure 'ollama' itself is installed/running separately.

# --- Optional/Utility Libraries (If used in scripts) ---
tqdm==4.67.1
pydantic==2.12.3